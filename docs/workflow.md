# 개발 워크플로 — 이 프로젝트의 작업 절차

첫 세션(2026-07-16)에서 정착된 절차의 핵심 기록. 새 작업도 이 흐름을 따른다.

## 1. 기능 개발 파이프라인

```
조사 → 설계 → 구현 → 검산 → (시각 변경 시) 로컬 검증 → 문서화 → 보고·확인 → 커밋 → 푸시·배포 확인
```

1. **조사**: 트렌드·사용 맥락을 웹으로 검증하고 [trend-research.md](./trend-research.md)에 출처와 함께 기록. 지식으로 채운 값은 "검증 전"임을 인지하고 이후 데이터로 교정.
2. **설계**: [design-principles.md](./design-principles.md)(게이미피케이션·어포던스·정직함)를 통과하는지 먼저 판정. 충돌하는 기법은 시작 전에 제거.
3. **구현**: 디자인 토큰·레이아웃 그리드(`theme.js` — `LAYOUT`)·공통 컴포넌트(`ui.js`·`MiniGame.js`)·표준 기법(색상 연결, Peak-End, 상대 좌표) 재사용.
4. **검산**: **요소 점유 좌표를 계산해 겹침 검사**(visual-polish 체크리스트), 레이아웃 총합 1280 예산 확인. 리팩터링이면 미사용 임포트·옛 시그니처 호출부 grep.
5. **로컬 검증(시각 변경 시)**: **정적 서버 + 헤드리스 Edge(720×1280) 스크린샷**으로 영향받는 씬을 전부 확인(2026-07-22 확립). 새 공통 컴포넌트는 임시 테스트 페이지로 렌더 확인 후 즉시 삭제. 사용자가 원하면 로컬 서버 주소를 제공해 배포 전 직접 확인 — 사용 후 서버 종료.
   - (2026-07-30 교정) 이 PC에 **Node는 있다** — PATH 미등록일 뿐: `C:\Program Files\nodejs`를 PATH에 추가하면 npm/npx 사용 가능 (자매 프로젝트 fries-get-cold에서 사용 중). PowerShell HttpListener 방식도 계속 유효.
   - 개선 후보: fries-get-cold의 `scripts/verify.mjs`(puppeteer-core로 자동 주파 + 스크린샷 + 수치 실측) 패턴을 도입하면 씬별 수동 스크린샷을 스크립트 한 번으로 대체할 수 있다.
     **2026-07-30 첫 적용**: 빙고판 사회자 모드 검증에 Node 정적 서버 + puppeteer-core 스크립트 사용 — 딥링크 진입·탭 시퀀스·localStorage 시드로 7개 상태(기본/마킹/설정/사회자/뽑기/팝업/새 판 확정)를 자동 촬영. 씬 상태를 미리 심을 땐 저장 스키마 검증(로더)을 통과하는 값으로.
6. **문서화**: 코드와 **같은 커밋**에서 관련 문서 갱신(game/development/…). 서드파티 추가 시 licenses.md **즉시** 기록.
7. **보고·확인**: 변경 요약 + 테스트 포인트를 보고하고 사용자 확인을 기다린다(진단만 요청받았으면 "적용해줘" 전에 수정하지 않음).
8. **커밋**: "커밋해줘" 승인 후 — Conventional Commits + 한국어, Co-Authored-By 없음, 커밋 전 상업 3문 점검([git.md](./git.md)). **커밋까지만 — 푸시는 별도 승인**.
9. **푸시·배포 확인**: "푸시해줘" 승인 후 푸시(Pages 반영 약 20초–2분 안내), curl로 핵심 파일 라이브 반영 폴링. 삭제한 파일은 404 확인. 띄워 둔 임시 서버·파일 정리.

## 2. 릴리스 절차

`/release` 커맨드(.claude/commands/release.md)로 자동화:
마지막 태그 이후 커밋 검토 → SemVer 결정 → CHANGELOG 작성 → README·메타·og 이미지 **숫자/문구 정합** → 커밋 → `vX.Y.Z` 주석 태그 → 배포 확인.

## 3. 이 프로젝트의 고정 규칙 (세션에서 확립)

- **정직함 최우선**: 화면=결과(정직한 매핑), 확률 공개(피티는 문서+화면 예고), 가짜 연출 금지, 카운트다운 같은 도박식 긴급함 금지.
- **색상 연결**: 결과·피드백 색 = 출처 요소의 색(visual-polish §3-1a). 참가자·칸·이빨 전부.
- **카피 톤**: "무료·설치 없이·지금 바로" 등 배너식 표현 금지 — 경험을 말한다(§3-5).
- **멀티 디바이스**: 상대 좌표, 터치 타깃 설계 88px+, hover는 보조.
- **상업 안전 상시**: 상업 허용 라이선스만·즉시 기록, 자산은 자체 제작 우선(이미지 = PowerShell System.Drawing, 소리 = Web Audio 합성, 아이콘 = 코드/이모지 텍스처).
- **시간대 시스템**: `timeOfDay.js` 단일 소스 — 분위기·인사말·메뉴·허브 라벨이 전부 여기서 파생(불일치 방지).

## 4. 배운 것 (실수 → 규칙)

| 사건 | 규칙화 |
|------|--------|
| 룰렛 포인터가 제목을 가림 | 요소 좌표 점유 범위 검산을 체크리스트化 |
| AdSense 미리보기에서 정적 페이지 이모지가 '??'로 깨져 저품질처럼 보임 (2026-08-01) | **정적 HTML 페이지에는 이모지 금지** — 심사·크롤러 렌더러는 이모지 폰트가 없다. 버튼 지칭은 텍스트 이름만("메뉴 편집"). 캔버스 안 이모지는 무관하므로 유지. **확장(2026-08-17): 발음부호가 붙은 라틴 문자도 피한다**(`Poláček` → `Spotify Engineering Blog`) — 같은 부류의 깨짐 위험. `×`·`÷` 등 기존에 쓰던 수학 기호는 무관 |
| 안내 페이지 문구가 실제 코드와 어긋난 채 배포될 뻔 (조 배정 60명 표기 등, 2026-08-01) | 대외 안내 문구는 **씬 코드의 상수·동작과 대조 검증** 후 배포 — 수치·버튼명·규칙은 코드가 원본 |
| 허브 라벨 "점심"이 실제(저녁)와 불일치 | 파생 값은 공용 모듈에서만 계산 |
| 러시안 룰렛 발사 칸 오프바이원 | 회전 **후** 상태 기준으로 판정·표시 |
| "설치 없이 무료" 문구 지적 | 카피 톤 규칙 신설 |
| 8번째 게임 추가 시 레이아웃 초과 | 추가 전 총합 계산(1280 예산) |
| 버튼 연타·동시 탭으로 씬 겹침/크래시 | **씬 전환은 `leaving` 가드 필수** — `scene.start` 호출부는 첫 탭만 통과(`MiniGame` 뒤로가기·허브 `startGame`) |
| 핀볼 강제 확정 후 null 참조로 루프 정지 | 같은 프레임에서 오브젝트를 없앴으면 **즉시 return**(후속 접근 금지) |
| 편집 패널 연 채 뒤로가기 → 재진입 시 패널이 안 열림 | 씬 인스턴스는 재사용된다 — **오버레이·모달 참조는 onCreate에서 초기화** |
| 씬마다 레이아웃 수치가 조금씩 어긋남(버튼 y 1100/1104, 모달 제목 36/38/40px 등) | **레이아웃은 `LAYOUT` 토큰, 헤더·링크·모달·칩은 공통 컴포넌트**(`makeHeader`·`makeSubLink`·`makeModal`·`chipFlow`)만 사용 — 씬에서 수치 하드코딩 금지(visual-polish §3-4a) |
| 검증용 임시 기본값(사회자 모드 켬)이 코드에 남은 채 세션 종료 — 새 사용자 기본이 스펙과 어긋날 뻔 | 임시 값은 주석에 **`[임시]` 마커 필수**, 마무리 검산에서 `grep '\[임시\]' src/` **0건** 확인 (/ship §1에 편입) |
| 한국어 분량을 `wc -c`(바이트)로 재고 글자 수로 보고 — 실제의 3배로 착각 (2026-08-17) | **분량 측정은 `wc -m`**. UTF-8 한국어는 글자당 3바이트라 바이트 수는 의미가 없다 |
| AdSense 1차 대응이 "크롤러가 읽는 HTML"만 고치고 재반려 (2026-08-17) | 심사·SEO 대응은 **렌더링된 화면 기준**으로 판정한다. 전면 캔버스 아래 DOM에 텍스트를 넣어도 리뷰어 화면에는 없는 것 — 첫 화면에서 도달 가능한지, 모바일에서 스크롤이 되는지까지 실측(puppeteer로 좌표·가시성 확인) |
| 도구별 안내 12페이지를 같은 h2 골격으로 양산 → "내용이 빈약한 콘텐츠" 판정 (2026-08-17) | 페이지를 세트로 만들 때 **골격을 서로 다르게** 한다. 동형 템플릿 반복은 자동 생성 콘텐츠의 시그니처 |
| 내부 개발 문서(`docs/*.md`·`CLAUDE.md`)가 Jekyll을 통해 SEO 태그까지 붙은 정식 페이지 18개로 **공개 배포되고 있었음** — 상업화 전략 문서 포함 (2026-08-17) | GitHub Pages는 **저장소 전체를 서빙하고 Jekyll이 기본 활성**이다. 루트 `.nojekyll` + `robots.txt Disallow`로 개발 문서를 사이트에서 분리한다. **"저장소에 넣는다 = 웹에 공개한다"**를 전제로 문서를 쓴다 |
| 홈의 안내 띠가 화면 밖으로 밀려 있었음 — 블록 요소 사이 **공백 텍스트 노드가 27px 줄 박스**를 만들고, `height`에 테두리가 더해져(box-sizing 미지정) 합이 뷰포트를 넘었다 (2026-08-17) | `100dvh`를 나눠 쓰는 레이아웃은 **합이 정확히 맞는지 실측**한다(`getBoundingClientRect`로 각 줄의 top/bottom). 고정 높이 요소에는 `box-sizing: border-box`, 세로 스택은 **body를 플렉스 컬럼**으로 두어 공백 노드를 무시시킨다 |
| AdSense **'광고 설정 미리보기'에서 홈이 빈 화면**으로 보였음 — 위아래 띠만 남고 가운데는 검은 상자 (2026-08-17) | 첫 화면이 캔버스면 **스크립트가 실행되지 않는 렌더러에는 아무것도 없다**(크롤러·CDN 차단도 같은 결과). 캔버스 자리에 **서버가 그대로 내려주는 정적 폴백**을 깔고 부팅이 확인된 시점에 제거한다. 대체 문구를 페이지 맨 끝(`<noscript>`)에 두면 **첫 화면 밖으로 밀려** 아무 역할도 못 한다 — 폴백은 캔버스와 **같은 자리**에 있어야 한다 |
| 그 미리보기의 원인을 '스크립트 미실행'으로 단정했다가, 미리보기 마크업을 직접 보고 **틀렸음이 드러남** (2026-08-17) | 미리보기는 페이지를 여는 것이 아니라 **구글 렌더러가 JS를 실행한 뒤의 DOM을 직렬화해 `<script>`를 걷어내고 `srcdoc`으로 재생**하는 구조였다(스냅샷에 Phaser가 만든 캔버스와 `transform: scale(...)`이 그대로 있었다). **캔버스 픽셀은 HTML로 직렬화되지 않으므로** 어떤 캔버스 사이트든 여기서는 빈 사각형이다. 교훈 둘 — ① 재현만으로 원인을 확정하지 않는다(같은 증상에 여러 경로가 있다), ② 도구가 무엇을 하는지는 **도구가 만든 마크업**을 보고 판단한다 |
| 그 빈 화면을 없애려 폴백을 **캔버스 뒤에 숨기는** 안을 검토 (2026-08-17) | 채택하지 않았다. 실측하면 재생 화면에는 글이 보이지만(빈 캔버스는 투명) 실제 화면에서는 레터박스로 새어 나와, 막으려면 **불투명한 캔버스 밑에 진짜 텍스트를 정확히 숨기는** 구조가 된다. 심사 중 '숨긴 텍스트' 리스크이자 **정직함 원칙 위반** — 화면과 DOM을 다르게 만드는 해법은 쓰지 않는다. 대신 **모두에게 보이는 콘텐츠**를 첫 화면 아래에 둔다 |
| 그 빈 화면의 또 다른 재현 경로: **CDN(jsdelivr) 한 곳이 막히면 사이트 전체가 검은 화면** (2026-08-17) | 화면이 뜨는 데 필수인 라이브러리는 **자체 호스팅**한다(단일 실패점 제거 + SRI 문제 소멸). 번들하면 CDN 원본의 라이선스 고지가 사라지므로 **파일 머리 배너 + `vendor/LICENSE-*.txt` 사본**을 함께 둔다(MIT 조건) |
| 안내 페이지의 확률 서술이 틀린 채 배포돼 있었음 — "나눠떨어지지 않는 인원은 걸릴 때까지 돌면 확률이 같다"(러시안·악어, 2026-08-17) | **확률 주장은 반례를 직접 계산해 검증**한다. 6칸을 4명이 돌면 앞 두 사람이 2배 불리 — 코드 상수(칸 수)와 진행 방식을 함께 대입해 봐야 드러난다. 안내 문구 검증에 "수치·버튼명·규칙"뿐 아니라 **확률·공정성 주장**을 항목으로 추가 |

> 세부 이력은 git log와 [CHANGELOG.md](../CHANGELOG.md) 참고. 이 문서는 "무엇을"이 아니라 **"어떻게"**를 기록한다.
